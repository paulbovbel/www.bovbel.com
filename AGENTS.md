# Agent Instructions

- Do not deploy to AWS or mutate AWS resources from this repository.
  - Do not run commands such as `cdk deploy`.
  - It is acceptable to run read-only AWS commands and local validation commands such as `cdk synth` or `cdk diff`.
