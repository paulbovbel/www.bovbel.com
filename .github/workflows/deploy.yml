name: Deploy www.bovbel.com

on:
  push:
    branches:
      - master
  workflow_dispatch: {}

concurrency:
  group: deploy-www-bovbel-com
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    name: Deploy www.bovbel.com
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v4

    - name: Set up Python
      run: uv python install 3.11

    - name: Install dependencies
      run: uv sync

    - name: Run pre-deploy tests
      env:
        RESUME_DOC_ID: ${{ secrets.RESUME_DOC_ID }}
      run: uv run pytest test.py -v -m pre_deploy --junit-xml=results-pre-deploy.xml

    - name: Publish pre-deploy test results
      uses: mikepenz/action-junit-report@v6
      if: always()
      with:
        report_paths: results-pre-deploy.xml
        fail_on_failure: true
        check_name: Pre-deploy Tests

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v5
      with:
        role-to-assume: ${{ secrets.DEPLOY_ROLE_ARN }}
        aws-region: us-east-2

    - name: Upload to S3
      env:
        RESUME_DOC_ID: ${{ secrets.RESUME_DOC_ID }}
      run: uv run python deploy.py --stack www-bovbel-com

    - name: Run post-deploy tests
      run: uv run pytest test.py -v -m post_deploy --junit-xml=results-post-deploy.xml

    - name: Publish post-deploy test results
      uses: mikepenz/action-junit-report@v6
      if: always()
      with:
        report_paths: results-post-deploy.xml
        fail_on_failure: true
        check_name: Post-deploy Tests
